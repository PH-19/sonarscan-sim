# sonarscan-sim

This repository contains the SonarScan simulator used in the CoAqua paper.

## Running locally

### Prerequisites

- Node.js

Install the dependencies:

```bash
npm install
```

Start the simulator:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in a browser.
